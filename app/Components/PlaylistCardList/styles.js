import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    marginLeft: 15
  },
  headerText: {
    fontSize: 20,
    fontWeight: 'bold',
    paddingTop: 10,
    paddingBottom: 10
  }
});

export default styles;

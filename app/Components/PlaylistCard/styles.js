import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {
    width: 100,
    paddingRight: 10
  },
  imageContainer: {
    width: 80,
    height: 80,
    borderRadius: 10
  },
  titleText: {
    fontWeight: 'bold',
    paddingBottom: 10
  },
  descText: {
    color: '#ccc',
  }
});

export default styles;
